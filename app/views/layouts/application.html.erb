<!DOCTYPE html>
<html>
<head>
  <title>Bucket2015</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>
<h1>버킷2015</h1>

<div>
  <% if user_signed_in? %>
    <a href="<%= destroy_user_session_path %>" data-method="delete">로그아웃</a>
    <img width="50px" height="50px" src="<%= current_user.avatar %>"/>
  <% else %>
    <a href="<%= new_user_session_path %>">로그인</a> <a href="<%= new_user_registration_path %>">회원가입</a>
  <% end %>
</div>
<br>

<div>
  <a href="<%= cards_path %>">사진피드</a> <a href="<%= projects_path %>">집들이피드</a>
</div>

<%= yield %>
<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>

</body>
</html>
